This folder contains MySQL triggers used to enforce automatic database behavior.

Included:
- Inventory update trigger executed after new order items are inserted

The trigger serves as a safety mechanism to maintain consistency even if orders bypass the stored procedures.
